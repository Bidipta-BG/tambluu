# Shared TypeScript types and interfaces for the Tambluu storefront.
#
# Domain types that are *only* relevant to the API layer live in src/lib/api.ts.
# Types used across multiple components or pages should be defined here.
